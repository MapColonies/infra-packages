## API Report File for "@map-colonies/drizzle-utils"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { Column } from 'drizzle-orm';
import type { commonDbFullV1Type } from '@map-colonies/schemas';
import type { DrizzleQueryError } from 'drizzle-orm';
import type { NodePgDatabase } from 'drizzle-orm/node-postgres';
import type { PgTable } from 'drizzle-orm/pg-core';
import { Pool } from 'pg';
import { PoolConfig } from 'pg';
import type { SQL } from 'drizzle-orm';
import type { Subquery } from 'drizzle-orm';

// @public
export function createConnectionOptions(dbOptions: commonDbFullV1Type): PoolConfig;

// @public
export function createDatesComparison(column: Column, earlyDate?: Date, laterDate?: Date): SQL | undefined;

// @public
export const DEFAULT_PAGE_SIZE = 10;

// @public
export function healthCheck(connection: Pool, dbConnectionTimeout?: number): () => Promise<void>;

// @public
export function initConnection(dbConfig: commonDbFullV1Type): Promise<Pool>;

// @public
export function isDrizzleQueryError(err: unknown): err is DrizzleQueryError;

// @public
export interface PaginationParams {
    // (undocumented)
    page: number;
    // (undocumented)
    pageSize: number;
}

// @public
export function paginationParamsToOffsetAndLimit(paginationParams?: PaginationParams): {
    limit: number;
    offset: number;
};

// @public
export const pgErrorCodes: {
    UNIQUE_VIOLATION: string;
};

// @public
export function runMigrations(drizzle: NodePgDatabase, migrationsSchema: string, baseFolder?: string): Promise<void>;

// @public
export function sortOptionParser<T extends object>(sortArray: string[] | undefined, sortFieldsMap: Map<string, keyof T>): SortOptions<T>;

// @public
export type SortOptions<T extends object> = {
    [key in keyof T]?: 'asc' | 'desc';
};

// @public
export function sortOptionsToOrderBy<T extends PgTable | Subquery>(tableDefinition: T, sortOptions: SortOptions<T>): SQL[];

// @public
export class SortQueryInvalidFieldError extends Error {
    constructor(message: string);
}

// @public
export class SortQueryInvalidOrderError extends Error {
    constructor(message: string);
}

// @public
export class SortQueryRepeatError extends Error {
    constructor(message: string);
}

// @public
export class SslValidationError extends Error {
    constructor(message: string);
}

// @public
export function validateSslCerts(key: Buffer, cert: Buffer, ca: Buffer): void;

// (No @packageDocumentation comment for this package)

```
